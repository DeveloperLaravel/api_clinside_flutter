import 'package:flutter/material.dart';

class MuitlSelection extends StatelessWidget {
  const MuitlSelection({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(backgroundColor: Colors.grey.shade900);
  }
}
